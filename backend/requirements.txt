# Core API
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic-settings==2.7.1
httpx==0.28.1

# Retrieval / store (added in later phases)
# numpy, psycopg[binary], pgvector, fastembed, onnxruntime, beautifulsoup4
